import {display_toast, create_message} from '/static/js/toaster.js'
const { ref } = Vue
export default {
    delimiters: ['[[', ']]'],
	props: {
		task: Object
	},
	setup(props) {

        
		return {
            
		}
    },
	template: `
	<i>No info actually</i>
    `
}