"""Add queue_admin and queuer to Role model

Revision ID: queue_roles_001
Revises: 7437a55c703f
Create Date: 2026-01-29 00:00:00.000000

"""
from alembic import op
import sqlalchemy as sa


# revision identifiers, used by Alembic.
revision = 'queue_roles_001'
down_revision = '7437a55c703f'
branch_labels = None
depends_on = None


def upgrade():
    # ### commands auto generated by Alembic - please adjust! ###

    bind = op.get_bind()
    inspector = sa.inspect(bind)
    role_columns = [col['name'] for col in inspector.get_columns('role')]
    
    with op.batch_alter_table('role', schema=None) as batch_op:
        if 'queue_admin' not in role_columns:
            batch_op.add_column(sa.Column('queue_admin', sa.Boolean(), nullable=True, default=False))
        if 'queuer' not in role_columns:
            batch_op.add_column(sa.Column('queuer', sa.Boolean(), nullable=True, default=False))

    # ### end Alembic commands ###


def downgrade():
    # ### commands auto generated by Alembic - please adjust! ###

    bind = op.get_bind()
    inspector = sa.inspect(bind)
    role_columns = [col['name'] for col in inspector.get_columns('role')]
    
    with op.batch_alter_table('role', schema=None) as batch_op:
        if 'queuer' in role_columns:
            batch_op.drop_column('queuer')
        if 'queue_admin' in role_columns:
            batch_op.drop_column('queue_admin')

    # ### end Alembic commands ###
