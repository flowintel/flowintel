"""empty message

Revision ID: 7760a877dff6
Revises: 3a0416665c4b
Create Date: 2024-10-17 11:16:03.447654

"""
from alembic import op
import sqlalchemy as sa
from sqlalchemy import inspect


# revision identifiers, used by Alembic.
revision = '7760a877dff6'
down_revision = '3a0416665c4b'
branch_labels = None
depends_on = None


def upgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    # Check if column exists before adding
    bind = op.get_bind()
    inspector = inspect(bind)
    existing_columns = [col['name'] for col in inspector.get_columns('task__template')]
    
    if 'time_required' not in existing_columns:
        op.add_column('task__template', sa.Column('time_required', sa.String(), nullable=True))

    # ### end Alembic commands ###


def downgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    # Check if column exists before dropping
    bind = op.get_bind()
    inspector = inspect(bind)
    existing_columns = [col['name'] for col in inspector.get_columns('task__template')]
    
    if 'time_required' in existing_columns:
        op.drop_column('task__template', 'time_required')

    # ### end Alembic commands ###
